class Product {
    constructor(name,brand,price){
        this.productName = name
        this.productBran = brand
        this.productPrice = price
    }

    aplicarDesconto(percentage){
        let percentage = 10
    }
}

const purchaseDiscount = new productPrice("1000")
console.log("The product " + this.productName + " by " + this.productBrand + " and your original price is " + this.productPrice + ", but with 10% OFF " + this.productPrice / 10)